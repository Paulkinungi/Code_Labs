# Railway Nixpacks Configuration for CodeLabs

[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.install]
cmds = [
  'npm install',
  'cd client && npm install'
]

[phases.build]
cmds = [
  'cd client && npm run build'
]

[start]
cmd = 'node server/index.js'

[variables]
NODE_ENV = 'production'
